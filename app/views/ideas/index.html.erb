<!DOCTYPE html>
<html>
<head>
	<title>Ideator</title>
</head>
<body>
	<button type="button" class="btn btn-primary btn-lg btn_idea" data-toggle="modal" data-target="#myModal">
  Add Ideas
</button>


<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
        <h4 class="modal-title" id="myModalLabel">Generate Your Ideas</h4>
      </div>
      <div class="modal-body">
       	<%= simple_form_for Idea.new do |f| %>
  <%= f.input :description %>
  <%= f.input :author %>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <%= f.submit 'Submit', class: 'btn btn-primary' %>
      </div>
      <% end %>
    </div>
  </div>
</div>
	<% @ideas.each do|idea| %>
		<a href="/ideas/edit">Edit</a>
			<div class = "panel panel-default">
			<div class="panel-body">
				<blockquote>
				<p> <%= idea.description %></p>
				<small> <%= idea.author %> </small>
			</blockquote>
			</div>	
		</div>	
	<% end %>	
</body>
</html>
